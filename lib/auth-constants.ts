/** Name of the httpOnly cookie that holds the signed worker session. */
export const WORKER_COOKIE = "sc_worker";

/** Worker auto-logout window (spec: 5 minutes of inactivity). */
export const WORKER_SESSION_SECONDS = 60 * 5;
